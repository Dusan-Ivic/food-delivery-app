export enum UserType {
  Customer = 0,
  Partner = 1,
  Admin = 2,
}

export enum AllowedUserType {
  Customer = 0,
  Partner = 1,
}

export enum GrantType {
  UsernamePassword = 0,
  RefreshToken = 1,
}
