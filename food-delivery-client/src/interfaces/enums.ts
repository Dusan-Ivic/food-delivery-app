export enum StateStatus {
  None,
  Loading,
  Error,
  Success,
}

export enum PartnerStatus {
  Pending = 0,
  Rejected = 1,
  Accepted = 2,
  Suspended = 3,
}

export enum OrderStatus {
  Pending = 0,
  Canceled = 1,
  Completed = 2,
}
