version: "3"

services:
  db:
    image: postgis/postgis:15-3.4
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: FoodDeliveryDb
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - pgdata:/var/lib/postgresql/data

  backend:
    build:
      context: ./FoodDeliveryServer
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    environment:
      ConnectionStrings__FoodDeliveryDbConnectionString: Host=db;Database=FoodDeliveryDb;Username=postgres;Password=postgres;
    depends_on:
      - db

  frontend:
    build:
      context: ./food-delivery-client
      dockerfile: Dockerfile
      args:
        VITE_BACKEND_API_URL: http://localhost:8080
        VITE_GEOAPIFY_API_KEY: API_KEY
    ports:
      - "3000:3000"
    depends_on:
      - backend

volumes:
  pgdata:
